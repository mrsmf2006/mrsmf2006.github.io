# 🚀 راهنمای پوش کردن و استقرار روی GitHub Pages

پروژه شما به‌طور کامل برای انتشار روی **GitHub Pages** پیکربندی و آماده شده است. 

---

### ۱. دستورات لازم برای اتصال به مخزن گیت‌هاب و Push

اگر در گیت‌هاب خود یک ریپازیتوری جدید (مثلاً با نام `portfolio` یا `mrsmf2006.github.io`) ایجاد کرده‌اید، کافیست دستورات زیر را در ترمینال سیستم خود اجرا کنید:

```bash
# ۱. اضافه کردن ریموت مخزن گیت‌هاب (نام کاربری و نام ریپازیتوری خود را وارد کنید)
git remote add origin https://github.com/mrsmf2006/YOUR_REPO_NAME.git

# ۲. اطمینان از قرار داشتن روی شاخه main
git branch -M main

# ۳. پوش کردن کدها
git push -u origin main
```

---

### ۲. فعال‌سازی GitHub Pages در گیت‌هاب (تنها با چند کلیک)

پس از اجرای `git push`:

1. وارد صفحه ریپازیتوری خود در GitHub شوید.
2. روی تب **Settings** در بالای صفحه کلیک کنید.
3. از منوی سمت چپ، روی گزینه **Pages** کلیک کنید.
4. در بخش **Build and deployment**:
   - در منوی کشویی **Source**، گزینه **GitHub Actions** را انتخاب کنید.
   
*(یک فایل آماده در مسیر `.github/workflows/deploy.yml` قرار داده شده که به محض تغییرات یا اولین پوش، سایت را خودکار بیلد و با آدرس زنده فعال می‌کند).*

**روش جایگزین (Deploy from a branch):**
اگر مایلید بدون Actions مستقیماً از شاخه اصلی استفاده کنید:
- در منوی **Source** گزینه **Deploy from a branch** را انتخاب کنید.
- شاخه را روی `main` و مسیر را روی `/ (root)` قرار دهید و دکمه **Save** را بزنید.

---

### ۳. ویژگی‌های پیکربندی‌شده برای GitHub Pages در پروژه:
- ✅ فایل `.nojekyll` برای جلوگیری از تداخل Jekyll در پردازش پوشه‌ها و فایل‌های استاتیک.
- ✅ فایل `404.html` برای مدیریت خودکار مسیرهای SPA و ریدایرکت نرم.
- ✅ تنظیم `base: './'` در `vite.config.ts` برای سازگاری کامل هم با دامنه‌های شخصی و هم با ساب‌مسیرها (`username.github.io/repo-name/`).
- ✅ ورک‌فلو خودکار `.github/workflows/deploy.yml` جهت Build و Deploy اتوماتیک در هر Push.
